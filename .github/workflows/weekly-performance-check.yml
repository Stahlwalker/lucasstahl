name: Weekly Performance Check

# Runs every Sunday at 9 AM UTC (4 AM EST / 1 AM PST)
on:
  schedule:
    - cron: '0 9 * * 0'
  workflow_dispatch:  # Allows manual triggering from Actions tab

jobs:
  performance-check:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # Changed to write for committing results
      issues: write

    steps:
      - name: Check Performance - Home
        id: home
        run: |
          echo "Testing homepage..."
          RESULT=$(curl -s "https://www.googleapis.com/pagespeedonline/v5/runPagespeed?url=https://lukestahl.io/&strategy=mobile&category=performance&category=accessibility&category=best-practices&category=seo&key=${{ secrets.PAGESPEED_API_KEY }}")

          # Check if API returned an error
          ERROR=$(echo $RESULT | jq -r '.error.message // empty')
          if [ ! -z "$ERROR" ]; then
            echo "API Error: $ERROR"
            echo "performance=0" >> $GITHUB_OUTPUT
            echo "accessibility=0" >> $GITHUB_OUTPUT
            echo "best_practices=0" >> $GITHUB_OUTPUT
            echo "seo=0" >> $GITHUB_OUTPUT
            echo "lcp=0" >> $GITHUB_OUTPUT
            echo "cls=0" >> $GITHUB_OUTPUT
            echo "tbt=0" >> $GITHUB_OUTPUT
            exit 1
          fi

          PERF=$(echo $RESULT | jq -r '(.lighthouseResult.categories.performance.score // 0) * 100')
          A11Y=$(echo $RESULT | jq -r '(.lighthouseResult.categories.accessibility.score // 0) * 100')
          BP=$(echo $RESULT | jq -r '(.lighthouseResult.categories["best-practices"].score // 0) * 100')
          SEO=$(echo $RESULT | jq -r '(.lighthouseResult.categories.seo.score // 0) * 100')

          LCP=$(echo $RESULT | jq -r '.lighthouseResult.audits["largest-contentful-paint"].numericValue // 0')
          CLS=$(echo $RESULT | jq -r '.lighthouseResult.audits["cumulative-layout-shift"].numericValue // 0')
          TBT=$(echo $RESULT | jq -r '.lighthouseResult.audits["total-blocking-time"].numericValue // 0')

          echo "Performance: $PERF"
          echo "Accessibility: $A11Y"
          echo "Best Practices: $BP"
          echo "SEO: $SEO"

          echo "performance=$PERF" >> $GITHUB_OUTPUT
          echo "accessibility=$A11Y" >> $GITHUB_OUTPUT
          echo "best_practices=$BP" >> $GITHUB_OUTPUT
          echo "seo=$SEO" >> $GITHUB_OUTPUT
          echo "lcp=$LCP" >> $GITHUB_OUTPUT
          echo "cls=$CLS" >> $GITHUB_OUTPUT
          echo "tbt=$TBT" >> $GITHUB_OUTPUT

      - name: Check Performance - About
        id: about
        continue-on-error: true
        run: |
          echo "Testing about page..."
          RESULT=$(curl -s "https://www.googleapis.com/pagespeedonline/v5/runPagespeed?url=https://lukestahl.io/about&strategy=mobile&category=performance&category=accessibility&category=best-practices&category=seo&key=${{ secrets.PAGESPEED_API_KEY }}")

          PERF=$(echo $RESULT | jq -r '(.lighthouseResult.categories.performance.score // 0) * 100')
          A11Y=$(echo $RESULT | jq -r '(.lighthouseResult.categories.accessibility.score // 0) * 100')
          BP=$(echo $RESULT | jq -r '(.lighthouseResult.categories["best-practices"].score // 0) * 100')
          SEO=$(echo $RESULT | jq -r '(.lighthouseResult.categories.seo.score // 0) * 100')

          echo "performance=$PERF" >> $GITHUB_OUTPUT
          echo "accessibility=$A11Y" >> $GITHUB_OUTPUT
          echo "best_practices=$BP" >> $GITHUB_OUTPUT
          echo "seo=$SEO" >> $GITHUB_OUTPUT

      - name: Check Performance - Blog
        id: blog
        continue-on-error: true
        run: |
          echo "Testing blog page..."
          RESULT=$(curl -s "https://www.googleapis.com/pagespeedonline/v5/runPagespeed?url=https://lukestahl.io/blog&strategy=mobile&category=performance&category=accessibility&category=best-practices&category=seo&key=${{ secrets.PAGESPEED_API_KEY }}")

          PERF=$(echo $RESULT | jq -r '(.lighthouseResult.categories.performance.score // 0) * 100')
          A11Y=$(echo $RESULT | jq -r '(.lighthouseResult.categories.accessibility.score // 0) * 100')
          BP=$(echo $RESULT | jq -r '(.lighthouseResult.categories["best-practices"].score // 0) * 100')
          SEO=$(echo $RESULT | jq -r '(.lighthouseResult.categories.seo.score // 0) * 100')

          echo "performance=$PERF" >> $GITHUB_OUTPUT
          echo "accessibility=$A11Y" >> $GITHUB_OUTPUT
          echo "best_practices=$BP" >> $GITHUB_OUTPUT
          echo "seo=$SEO" >> $GITHUB_OUTPUT

      - name: Check Performance - Builds
        id: builds
        continue-on-error: true
        run: |
          echo "Testing builds page..."
          RESULT=$(curl -s "https://www.googleapis.com/pagespeedonline/v5/runPagespeed?url=https://lukestahl.io/builds&strategy=mobile&category=performance&category=accessibility&category=best-practices&category=seo&key=${{ secrets.PAGESPEED_API_KEY }}")

          PERF=$(echo $RESULT | jq -r '(.lighthouseResult.categories.performance.score // 0) * 100')
          A11Y=$(echo $RESULT | jq -r '(.lighthouseResult.categories.accessibility.score // 0) * 100')
          BP=$(echo $RESULT | jq -r '(.lighthouseResult.categories["best-practices"].score // 0) * 100')
          SEO=$(echo $RESULT | jq -r '(.lighthouseResult.categories.seo.score // 0) * 100')

          echo "performance=$PERF" >> $GITHUB_OUTPUT
          echo "accessibility=$A11Y" >> $GITHUB_OUTPUT
          echo "best_practices=$BP" >> $GITHUB_OUTPUT
          echo "seo=$SEO" >> $GITHUB_OUTPUT

      - name: Check Performance - Gems
        id: gems
        continue-on-error: true
        run: |
          echo "Testing gems page..."
          RESULT=$(curl -s "https://www.googleapis.com/pagespeedonline/v5/runPagespeed?url=https://lukestahl.io/gems&strategy=mobile&category=performance&category=accessibility&category=best-practices&category=seo&key=${{ secrets.PAGESPEED_API_KEY }}")

          PERF=$(echo $RESULT | jq -r '(.lighthouseResult.categories.performance.score // 0) * 100')
          A11Y=$(echo $RESULT | jq -r '(.lighthouseResult.categories.accessibility.score // 0) * 100')
          BP=$(echo $RESULT | jq -r '(.lighthouseResult.categories["best-practices"].score // 0) * 100')
          SEO=$(echo $RESULT | jq -r '(.lighthouseResult.categories.seo.score // 0) * 100')

          echo "performance=$PERF" >> $GITHUB_OUTPUT
          echo "accessibility=$A11Y" >> $GITHUB_OUTPUT
          echo "best_practices=$BP" >> $GITHUB_OUTPUT
          echo "seo=$SEO" >> $GITHUB_OUTPUT

      - name: Check Performance - Home Desktop
        id: home-desktop
        continue-on-error: true
        run: |
          echo "Testing homepage (desktop)..."
          RESULT=$(curl -s "https://www.googleapis.com/pagespeedonline/v5/runPagespeed?url=https://lukestahl.io/&strategy=desktop&category=performance&category=accessibility&category=best-practices&category=seo&key=${{ secrets.PAGESPEED_API_KEY }}")

          PERF=$(echo $RESULT | jq -r '(.lighthouseResult.categories.performance.score // 0) * 100')
          A11Y=$(echo $RESULT | jq -r '(.lighthouseResult.categories.accessibility.score // 0) * 100')
          BP=$(echo $RESULT | jq -r '(.lighthouseResult.categories["best-practices"].score // 0) * 100')
          SEO=$(echo $RESULT | jq -r '(.lighthouseResult.categories.seo.score // 0) * 100')

          LCP=$(echo $RESULT | jq -r '.lighthouseResult.audits["largest-contentful-paint"].numericValue // 0')
          CLS=$(echo $RESULT | jq -r '.lighthouseResult.audits["cumulative-layout-shift"].numericValue // 0')
          TBT=$(echo $RESULT | jq -r '.lighthouseResult.audits["total-blocking-time"].numericValue // 0')

          echo "performance=$PERF" >> $GITHUB_OUTPUT
          echo "accessibility=$A11Y" >> $GITHUB_OUTPUT
          echo "best_practices=$BP" >> $GITHUB_OUTPUT
          echo "seo=$SEO" >> $GITHUB_OUTPUT
          echo "lcp=$LCP" >> $GITHUB_OUTPUT
          echo "cls=$CLS" >> $GITHUB_OUTPUT
          echo "tbt=$TBT" >> $GITHUB_OUTPUT

      - name: Check Performance - About Desktop
        id: about-desktop
        continue-on-error: true
        run: |
          echo "Testing about page (desktop)..."
          RESULT=$(curl -s "https://www.googleapis.com/pagespeedonline/v5/runPagespeed?url=https://lukestahl.io/about&strategy=desktop&category=performance&category=accessibility&category=best-practices&category=seo&key=${{ secrets.PAGESPEED_API_KEY }}")

          PERF=$(echo $RESULT | jq -r '(.lighthouseResult.categories.performance.score // 0) * 100')
          A11Y=$(echo $RESULT | jq -r '(.lighthouseResult.categories.accessibility.score // 0) * 100')
          BP=$(echo $RESULT | jq -r '(.lighthouseResult.categories["best-practices"].score // 0) * 100')
          SEO=$(echo $RESULT | jq -r '(.lighthouseResult.categories.seo.score // 0) * 100')

          echo "performance=$PERF" >> $GITHUB_OUTPUT
          echo "accessibility=$A11Y" >> $GITHUB_OUTPUT
          echo "best_practices=$BP" >> $GITHUB_OUTPUT
          echo "seo=$SEO" >> $GITHUB_OUTPUT

      - name: Check Performance - Blog Desktop
        id: blog-desktop
        continue-on-error: true
        run: |
          echo "Testing blog page (desktop)..."
          RESULT=$(curl -s "https://www.googleapis.com/pagespeedonline/v5/runPagespeed?url=https://lukestahl.io/blog&strategy=desktop&category=performance&category=accessibility&category=best-practices&category=seo&key=${{ secrets.PAGESPEED_API_KEY }}")

          PERF=$(echo $RESULT | jq -r '(.lighthouseResult.categories.performance.score // 0) * 100')
          A11Y=$(echo $RESULT | jq -r '(.lighthouseResult.categories.accessibility.score // 0) * 100')
          BP=$(echo $RESULT | jq -r '(.lighthouseResult.categories["best-practices"].score // 0) * 100')
          SEO=$(echo $RESULT | jq -r '(.lighthouseResult.categories.seo.score // 0) * 100')

          echo "performance=$PERF" >> $GITHUB_OUTPUT
          echo "accessibility=$A11Y" >> $GITHUB_OUTPUT
          echo "best_practices=$BP" >> $GITHUB_OUTPUT
          echo "seo=$SEO" >> $GITHUB_OUTPUT

      - name: Check Performance - Builds Desktop
        id: builds-desktop
        continue-on-error: true
        run: |
          echo "Testing builds page (desktop)..."
          RESULT=$(curl -s "https://www.googleapis.com/pagespeedonline/v5/runPagespeed?url=https://lukestahl.io/builds&strategy=desktop&category=performance&category=accessibility&category=best-practices&category=seo&key=${{ secrets.PAGESPEED_API_KEY }}")

          PERF=$(echo $RESULT | jq -r '(.lighthouseResult.categories.performance.score // 0) * 100')
          A11Y=$(echo $RESULT | jq -r '(.lighthouseResult.categories.accessibility.score // 0) * 100')
          BP=$(echo $RESULT | jq -r '(.lighthouseResult.categories["best-practices"].score // 0) * 100')
          SEO=$(echo $RESULT | jq -r '(.lighthouseResult.categories.seo.score // 0) * 100')

          echo "performance=$PERF" >> $GITHUB_OUTPUT
          echo "accessibility=$A11Y" >> $GITHUB_OUTPUT
          echo "best_practices=$BP" >> $GITHUB_OUTPUT
          echo "seo=$SEO" >> $GITHUB_OUTPUT

      - name: Check Performance - Gems Desktop
        id: gems-desktop
        continue-on-error: true
        run: |
          echo "Testing gems page (desktop)..."
          RESULT=$(curl -s "https://www.googleapis.com/pagespeedonline/v5/runPagespeed?url=https://lukestahl.io/gems&strategy=desktop&category=performance&category=accessibility&category=best-practices&category=seo&key=${{ secrets.PAGESPEED_API_KEY }}")

          PERF=$(echo $RESULT | jq -r '(.lighthouseResult.categories.performance.score // 0) * 100')
          A11Y=$(echo $RESULT | jq -r '(.lighthouseResult.categories.accessibility.score // 0) * 100')
          BP=$(echo $RESULT | jq -r '(.lighthouseResult.categories["best-practices"].score // 0) * 100')
          SEO=$(echo $RESULT | jq -r '(.lighthouseResult.categories.seo.score // 0) * 100')

          echo "performance=$PERF" >> $GITHUB_OUTPUT
          echo "accessibility=$A11Y" >> $GITHUB_OUTPUT
          echo "best_practices=$BP" >> $GITHUB_OUTPUT
          echo "seo=$SEO" >> $GITHUB_OUTPUT

      - name: Generate Summary
        run: |
          echo "# 📊 Weekly Performance Report" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## Core Web Vitals" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📱 Mobile (Homepage)" >> $GITHUB_STEP_SUMMARY
          echo "- **LCP**: ${{ steps.home.outputs.lcp }}ms (threshold: <8000ms)" >> $GITHUB_STEP_SUMMARY
          echo "- **CLS**: ${{ steps.home.outputs.cls }} (threshold: <0.1)" >> $GITHUB_STEP_SUMMARY
          echo "- **TBT**: ${{ steps.home.outputs.tbt }}ms (target: <300ms)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🖥️ Desktop (Homepage)" >> $GITHUB_STEP_SUMMARY
          echo "- **LCP**: ${{ steps.home-desktop.outputs.lcp }}ms (threshold: <2500ms)" >> $GITHUB_STEP_SUMMARY
          echo "- **CLS**: ${{ steps.home-desktop.outputs.cls }} (threshold: <0.1)" >> $GITHUB_STEP_SUMMARY
          echo "- **TBT**: ${{ steps.home-desktop.outputs.tbt }}ms (target: <300ms)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## 📱 Mobile Lighthouse Scores" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Page | Performance | Accessibility | Best Practices | SEO |" >> $GITHUB_STEP_SUMMARY
          echo "|------|-------------|---------------|----------------|-----|" >> $GITHUB_STEP_SUMMARY
          echo "| Home | ${{ steps.home.outputs.performance }} | ${{ steps.home.outputs.accessibility }} | ${{ steps.home.outputs.best_practices }} | ${{ steps.home.outputs.seo }} |" >> $GITHUB_STEP_SUMMARY
          echo "| About | ${{ steps.about.outputs.performance }} | ${{ steps.about.outputs.accessibility }} | ${{ steps.about.outputs.best_practices }} | ${{ steps.about.outputs.seo }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Blog | ${{ steps.blog.outputs.performance }} | ${{ steps.blog.outputs.accessibility }} | ${{ steps.blog.outputs.best_practices }} | ${{ steps.blog.outputs.seo }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Builds | ${{ steps.builds.outputs.performance }} | ${{ steps.builds.outputs.accessibility }} | ${{ steps.builds.outputs.best_practices }} | ${{ steps.builds.outputs.seo }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Gems | ${{ steps.gems.outputs.performance }} | ${{ steps.gems.outputs.accessibility }} | ${{ steps.gems.outputs.best_practices }} | ${{ steps.gems.outputs.seo }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## 🖥️ Desktop Lighthouse Scores" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Page | Performance | Accessibility | Best Practices | SEO |" >> $GITHUB_STEP_SUMMARY
          echo "|------|-------------|---------------|----------------|-----|" >> $GITHUB_STEP_SUMMARY
          echo "| Home | ${{ steps.home-desktop.outputs.performance }} | ${{ steps.home-desktop.outputs.accessibility }} | ${{ steps.home-desktop.outputs.best_practices }} | ${{ steps.home-desktop.outputs.seo }} |" >> $GITHUB_STEP_SUMMARY
          echo "| About | ${{ steps.about-desktop.outputs.performance }} | ${{ steps.about-desktop.outputs.accessibility }} | ${{ steps.about-desktop.outputs.best_practices }} | ${{ steps.about-desktop.outputs.seo }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Blog | ${{ steps.blog-desktop.outputs.performance }} | ${{ steps.blog-desktop.outputs.accessibility }} | ${{ steps.blog-desktop.outputs.best_practices }} | ${{ steps.blog-desktop.outputs.seo }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Builds | ${{ steps.builds-desktop.outputs.performance }} | ${{ steps.builds-desktop.outputs.accessibility }} | ${{ steps.builds-desktop.outputs.best_practices }} | ${{ steps.builds-desktop.outputs.seo }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Gems | ${{ steps.gems-desktop.outputs.performance }} | ${{ steps.gems-desktop.outputs.accessibility }} | ${{ steps.gems-desktop.outputs.best_practices }} | ${{ steps.gems-desktop.outputs.seo }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "🔗 [View detailed reports on PageSpeed Insights](https://pagespeed.web.dev/?url=https://lukestahl.io/)" >> $GITHUB_STEP_SUMMARY

      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Save Results to JSON
        run: |
          TIMESTAMP=$(date -u +"%Y-%m-%dT%H:%M:%SZ")

          # Create JSON with results
          cat > astro-site/src/data/seo-results.json <<EOF
          {
            "lastUpdated": "$TIMESTAMP",
            "results": {
              "mobile": {
                "home": {
                  "performance": ${{ steps.home.outputs.performance }},
                  "accessibility": ${{ steps.home.outputs.accessibility }},
                  "bestPractices": ${{ steps.home.outputs.best_practices }},
                  "seo": ${{ steps.home.outputs.seo }},
                  "lcp": ${{ steps.home.outputs.lcp }},
                  "cls": ${{ steps.home.outputs.cls }},
                  "tbt": ${{ steps.home.outputs.tbt }}
                },
                "about": {
                  "performance": ${{ steps.about.outputs.performance }},
                  "accessibility": ${{ steps.about.outputs.accessibility }},
                  "bestPractices": ${{ steps.about.outputs.best_practices }},
                  "seo": ${{ steps.about.outputs.seo }}
                },
                "blog": {
                  "performance": ${{ steps.blog.outputs.performance }},
                  "accessibility": ${{ steps.blog.outputs.accessibility }},
                  "bestPractices": ${{ steps.blog.outputs.best_practices }},
                  "seo": ${{ steps.blog.outputs.seo }}
                },
                "builds": {
                  "performance": ${{ steps.builds.outputs.performance }},
                  "accessibility": ${{ steps.builds.outputs.accessibility }},
                  "bestPractices": ${{ steps.builds.outputs.best_practices }},
                  "seo": ${{ steps.builds.outputs.seo }}
                },
                "gems": {
                  "performance": ${{ steps.gems.outputs.performance }},
                  "accessibility": ${{ steps.gems.outputs.accessibility }},
                  "bestPractices": ${{ steps.gems.outputs.best_practices }},
                  "seo": ${{ steps.gems.outputs.seo }}
                }
              },
              "desktop": {
                "home": {
                  "performance": ${{ steps.home-desktop.outputs.performance }},
                  "accessibility": ${{ steps.home-desktop.outputs.accessibility }},
                  "bestPractices": ${{ steps.home-desktop.outputs.best_practices }},
                  "seo": ${{ steps.home-desktop.outputs.seo }},
                  "lcp": ${{ steps.home-desktop.outputs.lcp }},
                  "cls": ${{ steps.home-desktop.outputs.cls }},
                  "tbt": ${{ steps.home-desktop.outputs.tbt }}
                },
                "about": {
                  "performance": ${{ steps.about-desktop.outputs.performance }},
                  "accessibility": ${{ steps.about-desktop.outputs.accessibility }},
                  "bestPractices": ${{ steps.about-desktop.outputs.best_practices }},
                  "seo": ${{ steps.about-desktop.outputs.seo }}
                },
                "blog": {
                  "performance": ${{ steps.blog-desktop.outputs.performance }},
                  "accessibility": ${{ steps.blog-desktop.outputs.accessibility }},
                  "bestPractices": ${{ steps.blog-desktop.outputs.best_practices }},
                  "seo": ${{ steps.blog-desktop.outputs.seo }}
                },
                "builds": {
                  "performance": ${{ steps.builds-desktop.outputs.performance }},
                  "accessibility": ${{ steps.builds-desktop.outputs.accessibility }},
                  "bestPractices": ${{ steps.builds-desktop.outputs.best_practices }},
                  "seo": ${{ steps.builds-desktop.outputs.seo }}
                },
                "gems": {
                  "performance": ${{ steps.gems-desktop.outputs.performance }},
                  "accessibility": ${{ steps.gems-desktop.outputs.accessibility }},
                  "bestPractices": ${{ steps.gems-desktop.outputs.best_practices }},
                  "seo": ${{ steps.gems-desktop.outputs.seo }}
                }
              }
            },
            "history": []
          }
          EOF

          echo "✅ Results saved to seo-results.json"

      - name: Commit and Push Results
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add astro-site/src/data/seo-results.json
          git diff --staged --quiet || git commit -m "📊 Update SEO performance results - $(date -u +"%Y-%m-%d %H:%M UTC")"
          git push

      - name: Performance Summary
        run: |
          echo "## 📊 Performance Summary"
          echo ""
          echo "### 📱 Mobile Scores"
          echo "- Home: ${{ steps.home.outputs.performance }}"
          echo "- About: ${{ steps.about.outputs.performance }}"
          echo "- Blog: ${{ steps.blog.outputs.performance }}"
          echo "- Builds: ${{ steps.builds.outputs.performance }}"
          echo "- Gems: ${{ steps.gems.outputs.performance }}"
          echo ""
          echo "### 🖥️ Desktop Scores"
          echo "- Home: ${{ steps.home-desktop.outputs.performance }}"
          echo "- About: ${{ steps.about-desktop.outputs.performance }}"
          echo "- Blog: ${{ steps.blog-desktop.outputs.performance }}"
          echo "- Builds: ${{ steps.builds-desktop.outputs.performance }}"
          echo "- Gems: ${{ steps.gems-desktop.outputs.performance }}"
          echo ""
          echo "### 🎯 Core Web Vitals (Mobile)"
          echo "- LCP: ${{ steps.home.outputs.lcp }}ms"
          echo "- CLS: ${{ steps.home.outputs.cls }}"
          echo "- TBT: ${{ steps.home.outputs.tbt }}ms"
          echo ""
          echo "### 🎯 Core Web Vitals (Desktop)"
          echo "- LCP: ${{ steps.home-desktop.outputs.lcp }}ms"
          echo "- CLS: ${{ steps.home-desktop.outputs.cls }}"
          echo "- TBT: ${{ steps.home-desktop.outputs.tbt }}ms"
          echo ""
          echo "✅ Weekly performance check complete!"
          echo "📊 Results saved to astro-site/src/data/seo-results.json"

      - name: Create Performance Report Issue
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          REPORT=$(cat <<'EOF'
          ## 📊 Weekly Performance Report - $(date '+%B %d, %Y')

          ### 📱 Mobile Performance Scores
          | Page | Score |
          |------|-------|
          | Home | ${{ steps.home.outputs.performance }} |
          | About | ${{ steps.about.outputs.performance }} |
          | Blog | ${{ steps.blog.outputs.performance }} |
          | Builds | ${{ steps.builds.outputs.performance }} |
          | Gems | ${{ steps.gems.outputs.performance }} |

          ### 🖥️ Desktop Performance Scores
          | Page | Score |
          |------|-------|
          | Home | ${{ steps.home-desktop.outputs.performance }} |
          | About | ${{ steps.about-desktop.outputs.performance }} |
          | Blog | ${{ steps.blog-desktop.outputs.performance }} |
          | Builds | ${{ steps.builds-desktop.outputs.performance }} |
          | Gems | ${{ steps.gems-desktop.outputs.performance }} |

          ### 🎯 Core Web Vitals

          **📱 Mobile (Homepage)**
          - **LCP**: ${{ steps.home.outputs.lcp }}ms (target: <2500ms)
          - **CLS**: ${{ steps.home.outputs.cls }} (target: <0.1)
          - **TBT**: ${{ steps.home.outputs.tbt }}ms (target: <300ms)

          **🖥️ Desktop (Homepage)**
          - **LCP**: ${{ steps.home-desktop.outputs.lcp }}ms (target: <2500ms)
          - **CLS**: ${{ steps.home-desktop.outputs.cls }} (target: <0.1)
          - **TBT**: ${{ steps.home-desktop.outputs.tbt }}ms (target: <300ms)

          ---

          🔗 [View Full Workflow Run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})
          🔗 [Test on PageSpeed Insights](https://pagespeed.web.dev/?url=https://lukestahl.io/)
          EOF
          )

          gh issue create \
            --title "📊 Weekly Performance Report - $(date '+%B %d, %Y')" \
            --body "$REPORT" \
            --label "performance" \
            --repo ${{ github.repository }}

      - name: Send Email via Resend
        env:
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          TO_EMAIL: ${{ secrets.NOTIFICATION_EMAIL }}
          FROM_EMAIL: ${{ secrets.FROM_EMAIL }}
        run: |
          SUBJECT="📊 Weekly Performance Report - $(date '+%B %d, %Y')"
          BODY="<h2>📊 Weekly Performance Report - $(date '+%B %d, %Y')</h2><h3>📱 Mobile Performance Scores</h3><table border=\"1\" cellpadding=\"8\" cellspacing=\"0\" style=\"border-collapse: collapse;\"><tr style=\"background: #f5f5f5;\"><th style=\"padding: 8px;\">Page</th><th style=\"padding: 8px;\">Score</th></tr><tr><td style=\"padding: 8px;\">Home</td><td style=\"padding: 8px;\">${{ steps.home.outputs.performance }}</td></tr><tr><td style=\"padding: 8px;\">About</td><td style=\"padding: 8px;\">${{ steps.about.outputs.performance }}</td></tr><tr><td style=\"padding: 8px;\">Blog</td><td style=\"padding: 8px;\">${{ steps.blog.outputs.performance }}</td></tr><tr><td style=\"padding: 8px;\">Builds</td><td style=\"padding: 8px;\">${{ steps.builds.outputs.performance }}</td></tr><tr><td style=\"padding: 8px;\">Gems</td><td style=\"padding: 8px;\">${{ steps.gems.outputs.performance }}</td></tr></table><h3>🖥️ Desktop Performance Scores</h3><table border=\"1\" cellpadding=\"8\" cellspacing=\"0\" style=\"border-collapse: collapse;\"><tr style=\"background: #f5f5f5;\"><th style=\"padding: 8px;\">Page</th><th style=\"padding: 8px;\">Score</th></tr><tr><td style=\"padding: 8px;\">Home</td><td style=\"padding: 8px;\">${{ steps.home-desktop.outputs.performance }}</td></tr><tr><td style=\"padding: 8px;\">About</td><td style=\"padding: 8px;\">${{ steps.about-desktop.outputs.performance }}</td></tr><tr><td style=\"padding: 8px;\">Blog</td><td style=\"padding: 8px;\">${{ steps.blog-desktop.outputs.performance }}</td></tr><tr><td style=\"padding: 8px;\">Builds</td><td style=\"padding: 8px;\">${{ steps.builds-desktop.outputs.performance }}</td></tr><tr><td style=\"padding: 8px;\">Gems</td><td style=\"padding: 8px;\">${{ steps.gems-desktop.outputs.performance }}</td></tr></table><h3>🎯 Core Web Vitals</h3><h4>📱 Mobile (Homepage)</h4><ul><li><strong>LCP:</strong> ${{ steps.home.outputs.lcp }}ms (target: &lt;2500ms)</li><li><strong>CLS:</strong> ${{ steps.home.outputs.cls }} (target: &lt;0.1)</li><li><strong>TBT:</strong> ${{ steps.home.outputs.tbt }}ms (target: &lt;300ms)</li></ul><h4>🖥️ Desktop (Homepage)</h4><ul><li><strong>LCP:</strong> ${{ steps.home-desktop.outputs.lcp }}ms (target: &lt;2500ms)</li><li><strong>CLS:</strong> ${{ steps.home-desktop.outputs.cls }} (target: &lt;0.1)</li><li><strong>TBT:</strong> ${{ steps.home-desktop.outputs.tbt }}ms (target: &lt;300ms)</li></ul><hr><p><a href=\"${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}\">View Full Workflow Run</a><br><a href=\"https://pagespeed.web.dev/?url=https://lukestahl.io/\">Test on PageSpeed Insights</a></p>"

          # Use jq to properly escape the JSON
          PAYLOAD=$(jq -n \
            --arg from "$FROM_EMAIL" \
            --arg to "$TO_EMAIL" \
            --arg subject "$SUBJECT" \
            --arg html "$BODY" \
            '{from: $from, to: [$to], subject: $subject, html: $html}')

          echo "Sending email from: $FROM_EMAIL to: $TO_EMAIL"
          RESPONSE=$(curl -w "\nHTTP_STATUS:%{http_code}" -X POST "https://api.resend.com/emails" \
            -H "Authorization: Bearer $RESEND_API_KEY" \
            -H "Content-Type: application/json" \
            -d "$PAYLOAD")

          echo "Resend API Response: $RESPONSE"

          HTTP_STATUS=$(echo "$RESPONSE" | grep HTTP_STATUS | cut -d':' -f2)
          if [ "$HTTP_STATUS" != "200" ]; then
            echo "Error: Email sending failed with status $HTTP_STATUS"
          else
            echo "Email sent successfully!"
          fi
